{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "mynumber-generator",
  "title": "My Number Generator",
  "description": "Generate dummy but checksum-valid My Numbers (Japanese Individual Numbers) for testing.",
  "icon": "extension-icon.png",
  "author": "upamune",
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "generate-mynumber",
      "title": "Generate My Numbers",
      "description": "Generate dummy My Numbers with a valid check digit and copy or paste them.",
      "mode": "no-view",
      "arguments": [
        {
          "name": "numberOfMyNumbersToGenerate",
          "placeholder": "Number of My Numbers",
          "type": "text",
          "required": false
        }
      ]
    }
  ],
  "preferences": [
    {
      "name": "defaultAction",
      "title": "Default Action",
      "description": "The default action to perform with the generated My Numbers.",
      "type": "dropdown",
      "required": false,
      "default": "copy",
      "data": [
        {
          "title": "Copy to Clipboard",
          "value": "copy"
        },
        {
          "title": "Paste to Current Window",
          "value": "paste"
        }
      ]
    },
    {
      "name": "hyphenSeparated",
      "title": "Format",
      "label": "Insert hyphens (XXXX-XXXX-XXXX)",
      "description": "Format the generated My Numbers with hyphens every 4 digits.",
      "type": "checkbox",
      "required": false,
      "default": false
    },
    {
      "name": "defaultNumberOfMyNumbers",
      "title": "Default Number of My Numbers",
      "description": "The number of My Numbers to generate when no argument is given.",
      "type": "textfield",
      "required": false,
      "default": "1"
    }
  ],
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "test": "node --test src/*.test.ts",
    "publish": "npx @raycast/api@latest publish"
  },
  "dependencies": {
    "@raycast/api": "^1.85.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "^22.0.0",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.0"
  }
}
